<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tab Suspended</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            margin: 0;
            padding: 20px;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }
        
        .container {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 600px;
        }
        
        h1 {
            font-size: 3em;
            margin: 0 0 20px 0;
        }
        
        .tab-info {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            padding: 20px;
            margin: 20px 0;
        }
        
        .tab-title {
            font-size: 1.5em;
            margin-bottom: 10px;
            word-break: break-word;
        }
        
        .tab-url {
            font-size: 0.9em;
            opacity: 0.8;
            word-break: break-all;
            margin-bottom: 15px;
        }
        
        .session-info {
            background: rgba(0, 0, 0, 0.2);
            border-radius: 8px;
            padding: 15px;
            margin: 20px 0;
        }
        
        .session-id {
            font-family: 'Courier New', monospace;
            font-size: 0.9em;
            background: rgba(255, 255, 255, 0.1);
            padding: 8px 12px;
            border-radius: 5px;
            display: inline-block;
            cursor: pointer;
            transition: all 0.3s ease;
            margin: 5px 0;
        }
        
        .session-id:hover {
            background: rgba(255, 255, 255, 0.2);
            transform: scale(1.02);
        }
        
        .tab-id {
            font-family: 'Courier New', monospace;
            font-size: 0.8em;
            opacity: 0.7;
            margin-top: 10px;
        }
        
        .restore-btn {
            background: rgba(255, 255, 255, 0.2);
            border: 2px solid rgba(255, 255, 255, 0.3);
            color: white;
            padding: 15px 30px;
            border-radius: 10px;
            font-size: 1.1em;
            cursor: pointer;
            transition: all 0.3s ease;
            margin: 20px 10px;
        }
        
        .restore-btn:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: translateY(-2px);
        }
        
        .copy-notification {
            position: fixed;
            top: 20px;
            right: 20px;
            background: rgba(0, 0, 0, 0.8);
            color: white;
            padding: 10px 20px;
            border-radius: 5px;
            display: none;
            z-index: 1000;
        }
        
        .memory-saved {
            font-size: 0.9em;
            opacity: 0.8;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>💤</h1>
        <h2>Tab Suspended</h2>
        
        <div class="tab-info">
            <div class="tab-title" id="tabTitle">Loading...</div>
            <div class="tab-url" id="tabUrl">Loading...</div>
            
            <div class="session-info">
                <div><strong>Session ID:</strong></div>
                <div class="session-id" id="sessionId" onclick="copySessionId()" title="Click to copy">
                    Loading...
                </div>
                <div class="tab-id">
                    <strong>Tab ID:</strong> <span id="tabId">Loading...</span>
                </div>
            </div>
        </div>
        
        <button class="restore-btn" onclick="restoreTab()">
            🔄 Restore Tab
        </button>
        
        <button class="restore-btn" onclick="openOptions()">
            ⚙️ Options
        </button>
        
        <div class="memory-saved">
            Memory saved by suspending this tab
        </div>
    </div>
    
    <div class="copy-notification" id="copyNotification">
        Session ID copied to clipboard!
    </div>
    
    <script>
        const urlParams = new URLSearchParams(window.location.search);
        const originalUrl = urlParams.get('uri') || urlParams.get('url');
        const tabTitle = urlParams.get('title') || 'Suspended Tab';
        const sessionId = urlParams.get('sessionId') || 'No Session ID';
        const tabId = urlParams.get('tabId') || 'Unknown';
        
        document.getElementById('tabTitle').textContent = decodeURIComponent(tabTitle);
        document.getElementById('tabUrl').textContent = decodeURIComponent(originalUrl || '');
        document.getElementById('sessionId').textContent = sessionId;
        document.getElementById('tabId').textContent = tabId;
        
        document.title = `Suspended: ${decodeURIComponent(tabTitle)}`;
        
        function copySessionId() {
            const sessionIdText = document.getElementById('sessionId').textContent;
            navigator.clipboard.writeText(sessionIdText).then(() => {
                showCopyNotification();
            }).catch(err => {
                const textArea = document.createElement('textarea');
                textArea.value = sessionIdText;
                document.body.appendChild(textArea);
                textArea.select();
                document.execCommand('copy');
                document.body.removeChild(textArea);
                showCopyNotification();
            });
        }
        
        function showCopyNotification() {
            const notification = document.getElementById('copyNotification');
            notification.style.display = 'block';
            setTimeout(() => {
                notification.style.display = 'none';
            }, 2000);
        }
        
        function restoreTab() {
            if (originalUrl) {
                window.location.href = decodeURIComponent(originalUrl);
            } else {
                alert('Cannot restore: Original URL not found');
            }
        }
        
        function openOptions() {
            chrome.runtime.sendMessage({action: 'openOptions'});
        }
        
        document.addEventListener('keydown', (e) => {
            if ((e.ctrlKey && e.key === 'r') || e.key === 'F5') {
                e.preventDefault();
                restoreTab();
            }
        });
    </script>
</body>
</html>
